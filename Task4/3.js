function calculateSupply(age, amount_per_day) {
    return document.write(`You will need ${((100-age)*365)*amount_per_day} leters of water to last you until the ripe old age of 100`)
}

calculateSupply(23,2)